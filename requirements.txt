# ============================================================
# Poseidon - ADB Power Tool
# Modernized Dependencies
# ============================================================

qrcode[pil] >= 7.4
requests >= 2.28.0
colorama >= 0.4.6
tqdm >= 4.65.0
rich >= 12.0.0
tabulate >= 0.9.0

# mitmproxy / ADB pivot support
mitmproxy >= 10.0.0

# OCR / vision / image handling
Pillow >= 10.0.0
pytesseract >= 0.3.10
opencv-python >= 4.8.0

# Web server for Web-GUI
fastapi >= 0.95.0
uvicorn >= 0.20.0

# Optional: data / export stack
pandas >= 2.0.0

# Reports/Exports are generated as Markdown/JSON with stdlib helpers; no extra package required.

